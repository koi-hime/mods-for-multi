Just clone this repo into your `mods` directory. Should look something like this.
![example of mods dir](mods-dir-ex.png)

You can also extract the zip instead if you wanna just download the zip instead of cloning.
